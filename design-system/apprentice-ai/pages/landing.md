# Landing Override

- 目标气质：课堂友好，但不是儿童产品；要有精致感、可信度和一点学术排版味道。
- 使用色板：Primary `#0D9488`，Secondary `#2DD4BF`，Accent `#F97316`，Background `#F4FBF9`，Text `#16322F`。
- 使用字体：标题 `Crimson Pro`，正文/UI `Atkinson Hyperlegible`。
- 结构：Hero -> 3 步教学闭环 -> 界面预览 -> 适用场景/信任说明 -> CTA。
- 禁止：紫色 AI 默认风、卡通插画堆砌、通用 SaaS 渐变按钮滥用。

